const teamsModel = require('../models/hackathonTeam.model')
const hackathonModel = require('../models')
const addTeamMember = async (userId, hackathonId, teamId) => {
    const getHackathonPromise = {}
}

module.exports = {}